import 'package:flutter/material.dart';

class Faker extends StatelessWidget {
  const Faker({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
